#ifndef SERVICE_HPP
#define SERVICE_HPP
#include <string>
#include <iostream>
class service
{
public:
    service();
    service(service const & rhs);
    ~service();
    service &operator=(service const & rhs);

private:

};

#endif