#ifndef OPERATOR_HPP
#define OPERATOR_HPP
#include <string>
#include <iostream>
class operator
{
public:
    operator();
    operator(operator const & rhs);
    ~operator();
    operator &operator=(operator const & rhs);

private:

};

#endif